package collections

/**
 * When we use functions like 'map {}', 'filter {}', they create new lists,
 * it takes resources and time. So, working on big list(1000+ elements),
 * it is quicker to use the 'toSequence()', 'sequenceOf()', 'asSequence()'.
 * But if the list is small, this 'toSequence' will make the work only slower
 */

fun main() {

}