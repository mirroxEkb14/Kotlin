package functions

/**
 * When we have an early sample or just a model of the project
 * we can use it or with unit tests
 */

fun main() {
    `we can write like this and nothing will happen to us`()
}

fun `we can write like this and nothing will happen to us`() {}